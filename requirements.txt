# Motor (extrator de PDF + gerador/validador de XML)
pymupdf==1.28.2
lxml>=5.0
# packtools: usar a versao do GitHub (a do PyPI e antiga e nao instala no Python 3.12)
packtools @ https://github.com/scieloorg/packtools/archive/refs/tags/4.16.0.zip
Pillow>=10
picles.plumber
setuptools

# Site (Fase 2): FastAPI + templates + upload
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
markdown>=3.6
